.row
  .col-md-12  
    .well.well-sm
      .media
        .media-left
          a(href='#')
            img.media-object.avatar(id='commentPicture' + id src='/img/profile_pic.png' width='64px' heigth='64px')
        .media-body
          h4.media-heading(id='commentUsername' + id) Username
          p #{comment.text}
